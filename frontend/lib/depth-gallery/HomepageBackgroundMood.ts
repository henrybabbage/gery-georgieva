/**
 * Homepage depth gallery: background/blobs derive from Sanity asset
 * `metadata.palette` (image sampling). While `false`, the scene uses a plain
 * white backdrop and carousel slides omit palette-driven `moodColors`.
 *
 * Set to `true` to restore sampled colors everywhere that reads this flag.
 */
export const SANITY_IMAGE_PALETTE_MOOD_FOR_HOMEPAGE_DEPTH_GALLERY = false
